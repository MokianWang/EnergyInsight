# ========== 核心依赖 ==========
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# ========== MCP 工具 ==========
langchain-mcp-adapters>=0.1.0

# ========== LLM 提供商 ==========
# Qwen (通义千问) 和 DeepSeek 均通过 OpenAI 兼容接口调用
# 已包含在 langchain-openai 中，无需额外依赖

# ========== PDF 解析 ==========
PyMuPDF>=1.24.0

# ========== 降级工具（MCP 不可用时使用） ==========
httpx>=0.27.0
beautifulsoup4>=4.12.0
ddgs>=6.0.0

# ========== 配置 ==========
python-dotenv>=1.0.0
pydantic>=2.0.0

# ========== Step 2: Knowledge Base ==========
pymilvus>=2.4.0
langchain-text-splitters>=0.3.0

# ========== Step 3: RAG Pipeline ==========
rank-bm25>=0.2.2          # BM25 关键词检索（纯 Python）
jieba>=0.42.1             # 中文分词
joblib>=1.4.0             # BM25 索引持久化
scikit-learn>=1.3.0       # TF-IDF 重排序（纯 Python）
# BGE Cross-Encoder 重排序 → 设置 RERANKER_BACKEND=bge 启用
# 需要: pip install optimum[onnxruntime] transformers torch
# 注意: torch 在 Python 3.14 上可能有兼容性问题

# ========== Step 5: 能源计算工具 ==========
pypsa>=0.30.0          # 电力系统优化（容量优化 + OPF + 时序仿真）
# renewables-ninja>=0.2  # [可选] 开源气象数据客户端（无需 API Key）
